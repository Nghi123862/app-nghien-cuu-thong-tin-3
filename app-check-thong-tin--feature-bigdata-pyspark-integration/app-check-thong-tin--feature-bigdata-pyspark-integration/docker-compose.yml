services:
  app:
    build:
      context: .
      dockerfile: app-check-thong-tin--feature-bigdata-pyspark-integration/Dockerfile
    container_name: app-check-thong-tin
    environment:
      - TZ=Etc/UTC
      - RESOLUTION=1366x768
    ports:
      - "6080:6080"  # noVNC (open in browser)
      - "5901:5901"  # VNC (optional)
    volumes:
      - ./app-check-thong-tin--feature-bigdata-pyspark-integration/data:/app/data
      - ./app-check-thong-tin--feature-bigdata-pyspark-integration/bigdata:/app/bigdata
    restart: unless-stopped
